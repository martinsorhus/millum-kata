import { User } from '../interfaces/user.interface';

/**
 * Write a function that merges two arrays of users
 * The returned product should be an array with odd indexes being users from users1,
 * and even indexes should be users from users2
 *
 * NB: If there is a difference in length in the two input arrays,
 * the returned array should not contain more items than double the length of the shortest
 *
 *  example input: [a1, a2, a3, a4, a4], [b1, b2, b3, b4, b5, b6]
 *  returned value [a1, b1, a2, b2, a3, b3, a4, b4]
 *
 */

export const third = (users1: User[], users2: User[]) => {
    return [];
};
