import { User } from '../interfaces/user.interface';

/*  Write a function that takes in an array of users
 *   The function should filter out the users with no userid,
 *   and sort the list alphabetically by name, BUT if any of the users are super admins, they
 *   should be at the top of the list and if there are more than 1 super admin,
 *   they should also be sorted alphabetically by name
 *   return the updated array
 */

export const first = (users: User[]) => {
    return [];
};
