/*  Write a function that takes in two arrays of users as parameters
 *   If both arrays contains a user with the same id, replace the name of the
 *   user in the first array with the name from the user with the same id in
 *   the second array.
 *   Arrays will not contain duplicate id's
 *   Return the updated first array
 */

import { User } from '@interfaces';

export const duplicateIds = (users1: User[], users2: User[]) => {
    return [];
};
